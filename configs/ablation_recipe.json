{
    "_description": "Training recipe sweep: optimizer, LR schedule, grad clipping, warmup. Focal loss locked (γ=1.0, α=0.5). 9 folds × 2 opt × 2 sched × 2 clip × 3 warmup = 216 jobs. warmup_epochs ignored when lr_schedule=none (36 redundant combos accepted).",
    "method": "feature_exp",
    "fold": [0, 1, 2, 3, 4, 5, 6, 7, 8],
    "feature_set": "all",
    "d_model": 1024,
    "n_layers": 3,
    "dropout": 0.3,
    "nhead": 4,
    "seq_len": 7,
    "batch_size": 32,
    "lr": 1e-3,
    "n_epochs": 80,
    "seed": 42,
    "data_path": "data/processed/patient_data_export_all9.pkl",
    "loss_type": "focal",
    "focal_gamma": 1.0,
    "focal_alpha": 0.5,
    "optimizer": ["adam", "adamw"],
    "lr_schedule": ["none", "cosine_warm"],
    "grad_clip": [0.0, 1.0],
    "warmup_epochs": [3, 5, 10],
    "executor": {
        "exec_name": "ablation",
        "output_dir": "outputs/ablations",
        "log_dir": "outputs/ablations/logs"
    },
    "slurm": {
        "cores": 4,
        "nodes": 1,
        "time": "0-04:00",
        "memory": "4G",
        "partition": "gpunodes",
        "account": "",
        "email": "",
        "email_type": "END,FAIL",
        "log_dir": "outputs/slurm_logs",
        "job_name": "abl_recipe",
        "exclude": "gpunode13",
        "constraint": "",
        "gres": "gpu:1",
        "julia_path": ""
    }
}
